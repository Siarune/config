## Siarune's config for [NixOS](https://nixos.org/).

Configuration is split between system-wide options in `sys` and userspace options in `usr`

`sys` and `usr` set sane defaults in `share`, and each machine has a folder for additions/overrides.
